# Supabase Configuration
# Get these values from https://supabase.com/dashboard > Settings > API
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Russia-reachable proxy (Phase 8.6 Phase 2). Set to your nginx reverse-proxy
# URL (e.g. https://api.example.com) or leave empty to disable.
SUPABASE_PROXY_URL=

# Sentry error reporting (https://sentry.io — get DSN from Project Settings > Client Keys)
# Leave empty to disable Sentry (e.g. in local development).
SENTRY_DSN=

# Environment
NODE_ENV=development
